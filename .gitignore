# Node
node_modules/
.npm/
dist/
build/

# Python
__pycache__/
*.py[cod]
*$py.class
venv/
env/
.venv/

# Project specifics
Archive/
*.STITCH_LOCK
_tome_master_cache.json
project_ledger.json
*.rtf
*.pdf
*.jpg
*.jpeg
*.png
*.webp

# Secrets & machine-bound licenses — NEVER commit these
.env
.env.*
backend/.env
backend/*.lic
*.lic
backend/settings.json
backend/settings.json.bak

# Diagnostic / audit artifacts — may contain keys or full request payloads.
# These are throwaway tool output; never commit (root cause of past leak risk).
backend/audit_*.json
backend/boardroom_audits_*/
backend/handshake_forensics.txt
backend/last_export_req.txt
backend/last_export_error.txt
backend/authorized_portfolio.txt
backend/files.txt
backend/files_clean.txt
engine_p*.txt
Gemini models from key

# OS generated files
.DS_Store
Thumbs.db

# License keygen (secret salt — never commit)
TomeMaster_Keygen.py

# Encrypted vault / session files
settings.enc
*.enc
.tome_session.json
api_usage_log.jsonl

# Build reference (large conversation log)
Gemini_history.txt

# Windows artifacts
*.lnk

# Runtime port-handshake signals (written/deleted by the launcher + run.py)
.sovereign_port
.frontend_port
